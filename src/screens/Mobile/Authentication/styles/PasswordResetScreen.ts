import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  card: {
    borderColor: "#141c3a",
    borderWidth: 1,
    padding: 20
  },
  back: {
    color: "#141c3a",
    fontSize: 16,
    fontWeight: "800",
    marginTop: 18,
    textAlign: "center"
  }
});

export default styles;
