import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  link: {
    color: "#16203f",
    fontSize: 16,
    fontWeight: "600",
    marginVertical: 18,
    textAlign: "center"
  }
});

export default styles;
