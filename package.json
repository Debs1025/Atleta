{
  "name": "atleta-backend-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Backend && npm install && npm run build",
    "start": "cd Backend && npm start"
  },
  "dependencies": {
    "expo": "^57.0.19"
  },
  "devDependencies": {
    "@types/react": "~19.2.4",
    "typescript": "~6.0.3"
  }
}
